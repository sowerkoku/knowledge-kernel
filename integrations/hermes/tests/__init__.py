# Hermes Integration Tests

"""
Run all tests:

```bash
cd integrations/hermes/tests
python -m pytest -v
```

Tests verify:
- Existence checks (cmdb_exists)
- Entity retrieval (cmdb_get)
- Binary assertions (cmdb_assert)
- Impact analysis (cmdb_impact)
- Pre-packaged context (cmdb_context)
"""